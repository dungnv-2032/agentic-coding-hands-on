# Install Bulky App

Bulky App runs as a hosted web app. This install guide carries enough prose across
the fixture's sections that a small `--budget` value forces the trim ladder to run
in the integration test, without needing a genuinely large fixture on disk.

## Web

Open the portal and sign in with your organization account.
